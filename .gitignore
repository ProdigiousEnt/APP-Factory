# macOS
.DS_Store

# Node modules (never commit)
node_modules/

# Build outputs (generated files)
dist/
build/
*.log

# iOS/Xcode (Capacitor generates these, don't commit)
ios/
android/

# Environment files (contains API keys - SECURITY CRITICAL)
.env
.env.local
.env.*.local
*.env

# Public repo clone (if you have it locally)
APP-Factory/

# Future app ideas (not ready for production)
aurahome-ai_-interior-designer/
chronos-weaver_-infinite-journeys/
commutecast-ai/
mailgenie-ai/
memegenius-ai/
sentix_-customer-sentiment-dashboard/
virtual-food-photographer/
Triviarc/
_Blueprints/
